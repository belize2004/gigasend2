import { SANITY_API_VERSION, SANITY_DATASET, SANITY_PROJECT_ID } from "@/lib/constant"

export const apiVersion = SANITY_API_VERSION;

export const dataset = SANITY_DATASET;

export const projectId = SANITY_PROJECT_ID;